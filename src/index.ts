import App from './app';
import './main.scss';

App.run(document.body);
//TODO: get rid of 'as' usage in TS (refactor code eliminating header, main and footer elems from HTML or adding checks for potential nullable values in queryselectors)
